# PDF for this exercise

Drop a PDF file here named `earth.pdf` (not committed — this folder should
stay out of git except for this README). The lesson's own example saves a
Wikipedia article about Earth as a PDF and summarizes it — any PDF works to
see the prompt run; the filename is just what `index.js` looks for.
