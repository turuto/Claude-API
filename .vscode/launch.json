{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug current exercise",
            "program": "${file}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
