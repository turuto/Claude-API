node_modules/
dist/
.env
*.log
.DS_Store

# generated exercise output (regenerated fresh by that exercise's own script)
exercises/06-prompt-eval-dataset/dataset.json
exercises/08-prompt-eval-modelGrader/dataset.json
exercises/08b-prompt-eval-codeGrader/dataset.json
exercises/09a-prompt-engineering/output/
exercises/09b-process/output/
exercises/09c-solution/output/

# NOTE: 07's dataset.json is a static copy of 06's output, not something its own
# script regenerates — it's committed as a fixture so that exercise can run
# standalone right after a clone, without needing 06 run first.

# personal local Claude Code permissions, not shared project config
.claude/settings.local.json
